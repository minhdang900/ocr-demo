# Server-side configuration (not exposed to client)
# These variables are only used on the server-side
GATEWAY_URL=http://localhost:3001

# Note: OCR_API_KEY is handled internally by the server-side API route
# and should not be exposed to the client-side environment
